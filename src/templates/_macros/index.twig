{% macro icon(config) %}
	{% include "uploadit/_macros/_icon" with (config ?? {}) only %}
{% endmacro %}

{% macro preview(config) %}
	{% include "uploadit/_macros/_preview" with (config ?? {}) only %}
{% endmacro %}
